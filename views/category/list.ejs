<%- include('../base/base_header'); -%> 

<body>
    <% for (var i = 0; i < categories.length; i++) { %>
        <div class="container">
            <div class="row">
                <p><%= categories[i]._id %> 
                    <%= categories[i].title %> 
                    <%= categories[i].name %> 
                    <%= categories[i].description %> 
                    <%= categories[i].release_date %></p>
            </div>
            
            <a class="btn-pink" href="/category/<%=categories[i]._id%>">Detalles</a>
        </div>

    <% } %>
</body>

<%- include('../base/base_footer'); -%>